<div class="container">
  <div class="card">
    <div class="row justify-content-center">
      <div class="col-6">
        <img
          class="card-img"
          src="{{user.image}}"
          style="width:350px;height:100%"
        />
      </div>
      <div class="col-sm-6 align-self-center">
        <div class="card-body-right">
          <h5 class="card-title">{{user.name}}</h5>
          <p class="card-text">{{user.email}}</p>
          <p class="card-text">{{user.Comments.length}}
            則評論，{{commtentsUndupRest.length}}已評論餐廳</p>
          <a href="/users/{{user.id}}/edit" class="btn btn-primary">edit</a>
        </div>
      </div>
    </div>
  </div>
  <div class="card my-5">
    <div class="card-header">
      <p>{{commtentsUndupRest.length}}已評論餐廳</p>
    </div>
    <div class="container d-flex my-5">
      {{#each commtentsUndupRest}}
        <a href="/restaurants/{{this.Restaurant.id}}">
          <img
            class="card-img mx-2"
            src="{{this.Restaurant.image}}"
            style="width:80px;height:100%"
          />
        </a>
      {{/each}}
    </div>
  </div>

  <div class="card my-5">
    <div class="card-header">
      <p>{{user.FavoritedRestaurants.length}} 收藏餐廳</p>
    </div>
    <div class="container d-flex my-5">
      {{#each user.FavoritedRestaurants}}
        <a href="/restaurants/{{this.id}}">
          <img
            class="card-img mx-2"
            src="{{this.image}}"
            style="width:80px;height:100%"
          />
        </a>
      {{/each}}
    </div>
  </div>

  <div class="card my-5">
    <div class="card-header">
      <p>{{user.Followers.length}} followings(追蹤者)</p>
    </div>
    <div class="container d-flex my-5">
      {{#each user.Followers}}
        <a href="/users/{{this.id}}">
          <img
            class="card-img mx-2"
            src="{{this.image}}"
            style="width:80px;height:100%"
          />
        </a>
      {{/each}}
    </div>
  </div>

  <div class="card my-5">
    <div class="card-header">
      <p>{{user.Followings.length}} Followers(追隨者)</p>
    </div>
    <div class="container d-flex my-5">
      {{#each user.Followings}}
        <a href="/users/{{this.id}}">
          <img
            class="card-img mx-2"
            src="{{this.image}}"
            style="width:80px;height:100%"
          />
        </a>
      {{/each}}
    </div>
  </div>
</div>